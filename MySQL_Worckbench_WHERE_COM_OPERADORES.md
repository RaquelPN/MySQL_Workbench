```MySQL

-- = igual | > menor | < maior | >= menor igual | <= maior igual | != ou <> diferente de

```
```MySQL
SELECT * FROM payment
WHERE amount = 0.99

```
```MySQL
SELECT * FROM payment
WHERE amount > 0.99

```
```MySQL
SELECT * FROM payment
WHERE amount < 0.99

```
```MySQL
SELECT * FROM address
 WHERE district != 'california'

```
```MySQL
SELECT * FROM address
 WHERE district = 'california'

```
